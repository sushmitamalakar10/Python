# Replace the double space from problem 3 with single space

name = "Ram  lives in  Ktm"
print(name.replace("  ", " ")) # name string is not changed, new string is formed

# Strings are immutable which means that we can't change them by running functions on them